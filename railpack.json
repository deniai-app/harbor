{
  "$schema": "https://schema.railpack.com",
  "buildAptPackages": [
    "git",
    "ca-certificates"
  ],
  "caches": {
    "bun": {
      "directory": "/root/.bun/install/cache",
      "type": "shared"
    }
  },
  "steps": {
    "install": {
      "inputs": [
        {
          "local": true,
          "include": [
            "."
          ]
        }
      ],
      "commands": [
        {
          "cmd": "cd apps/api && bun install --frozen-lockfile",
          "customName": "Install workspace dependencies"
        }
      ],
      "caches": [
        "bun"
      ]
    }
  },
  "deploy": {
    "startCommand": "bun run --filter api start",
    "aptPackages": [
      "git",
      "ca-certificates"
    ],
    "inputs": [
      {
        "step": "install",
        "include": [
          "."
        ]
      }
    ]
  }
}
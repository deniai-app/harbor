{
  "$schema": "https://schema.railpack.com",
  "buildAptPackages": [
    "git",
    "ca-certificates"
  ],
  "caches": {
    "bun": {
      "directory": "/root/.bun/install/cache",
      "type": "shared"
    }
  },
  "steps": {
    "install": {
      "commands": [
        "sh -c 'cd /app && bun install --frozen-lockfile --cwd apps/api'"
      ],
      "caches": [
        "bun"
      ]
    }
  },
  "deploy": {
    "startCommand": "sh -c 'cd /app/apps/api && bun run start'",
    "aptPackages": [
      "git",
      "ca-certificates"
    ],
    "inputs": [
      {
        "step": "install",
        "include": [
          "."
        ]
      }
    ]
  }
}
